/**
 * Client Loading Component
 * Loading state for client routes
 */

import Loading from "@/components/common/Loading";

export default function ClientLoading() {
  return <Loading variant="fullPage" size="large" tip="Đang tải..." />;
}

